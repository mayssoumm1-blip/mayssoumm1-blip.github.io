# include <stdio.h>
# include <math.h>
int main () 
{
    // sqrt(x): racine carrée de x
    // pow(x,n): x puissance n
    // round(x): arrondi de x
    // ceil(x)
    // floor(x)
    // abs(x): valeur absolue de x
    // log(x)
    return 0;
}